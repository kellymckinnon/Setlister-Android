<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ListView
        android:id="@+id/setlist"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <com.getbase.floatingactionbutton.FloatingActionsMenu
        android:id="@+id/multiple_actions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        fab:fab_addButtonColorNormal="@color/my_accent"
        fab:fab_addButtonColorPressed="@color/my_primary"
        android:layout_marginBottom="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginEnd="16dp">

        <com.getbase.floatingactionbutton.FloatingActionButton
            android:id="@+id/rdio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_rdio"
            fab:fab_colorNormal="@android:color/white"
            fab:fab_title="Action A"/>

        <com.getbase.floatingactionbutton.FloatingActionButton
            android:id="@+id/spotify"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            fab:fab_colorNormal="@android:color/black"
            android:src="@drawable/ic_spotify"
            fab:fab_colorPressed="#b283ff00"
            fab:fab_title="Action B"
            android:nestedScrollingEnabled="false"
            android:onClick="authenticateSpotify" />

    </com.getbase.floatingactionbutton.FloatingActionsMenu>


</RelativeLayout>
